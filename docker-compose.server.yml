version: '3'

services:
  app:
    image: cornellappdev/eatery-blue-backend:${IMAGE_TAG}
    env_file: .env
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    ports:
      - "8000:8000"
    restart: always